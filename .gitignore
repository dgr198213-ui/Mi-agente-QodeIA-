node_modules
.next
out
build
dist

# Variables de entorno con credenciales - NUNCA subir al repositorio
.env
.env.local
.env.development
.env.production
.env.staging

# Mantener solo el template como referencia
# .env.template  <-- este SI se puede subir (sin valores reales)

.DS_Store
*.log

# TypeScript build info (generado automáticamente, no necesita versionarse)
tsconfig.tsbuildinfo
